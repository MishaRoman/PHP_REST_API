{
    "autoload": {
        "psr-4": {
            "App\\": "./"
        }
    },
    "require-dev": {
        "phpunit/phpunit": "^9.6",
        "guzzlehttp/guzzle": "^7.5"
    },
    "require": {
        "vlucas/phpdotenv": "^5.5",
        "firebase/php-jwt": "^6.4"
    },
    "scripts": {
        "test": "./vendor/bin/phpunit --verbose tests",
        "test-tasks": "./vendor/bin/phpunit --bootstrap vendor/autoload.php --testsuite tasks"
    }
}
